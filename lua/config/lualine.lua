-- here is the configuration file for lualine, honestly I really like the default style with the dracula theme 
-- but you can modify it as you like it


require("lualine").setup({
    options = {
        theme = "dracula"
    }
})
